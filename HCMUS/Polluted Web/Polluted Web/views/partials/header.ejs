<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/x-icon" href="favicon.ico">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Knewave&family=Roboto+Mono:wght@500;700&family=Rubik+Mono+One&display=swap');

    body {
      font-family: 'Rubik Mono One', Verdana, sans-serif;
      margin: 0;
      padding: 0;
    }
    .background {
      --gradient-from-color: #113259;
      --gradient-color-stops: var(--gradient-from-color), var(--gradient-to-color, rgba(17, 50, 89, 0));
      background-image: linear-gradient(180deg, var(--gradient-color-stops));
    }
    .inp-container {
      background: url('https://teamtrees.org/img/header.png');
      background-repeat: no-repeat;
      background-size: cover;
      min-height: max-content;
      display: flex;
      flex-direction: column;
      flex-wrap: wrap;
      align-content: center;
      justify-content: flex-start;
      align-items: center;
      padding-bottom: 3.5em;
      border-bottom: 0.3rem solid black;
    }

    .inp-container label{
      font-weight: 400;
      font-size: x-large;
      padding-top: 1em;
      -webkit-text-stroke: 1px white;
      text-shadow: 1px 1px white;
    }

    .inp-name {
      font-family: 'Rubik Mono One';
      padding: 1em;
      margin-top: 1em;
      font-size: larger;
      min-width: 20em;
    }
    .inp-text {
      font-family: 'Rubik Mono One';
      padding: 1em;
      margin-top: 1em;
      font-size: large;
    }
    input:focus-visible, textarea:focus-visible {
      outline: none;
    }

    input[type=submit] {
      background-color: white;
      color: black;
      border: 0.3em solid black;
      padding: 1em;
      text-align: center;
      text-decoration: none;
      display: inline-block;
      cursor: pointer;
      transition-duration: 0.3s;
    }

    input[type=text], textarea {
      border: 0.3em solid black;
    }

    .btn {
      font-family: 'Rubik Mono One';
      margin-top: 1em;
      font-size: larger;
      font-weight: 400;
    }

    .btn:hover {
      background-color: black;
      color: white;
      /* background-color: rgb(212, 202, 202); */
    }

    .opinion-container {
      margin-top: 2.5em;
      display: flex;
      flex-direction: column;
      flex-wrap: nowrap;
      align-items: center; 
      justify-content: center;
    }

    .opinion-card {
      margin: 0.4em;
      padding: 1em 3em;
      min-width: 25em;
      border: 5px solid black;
      text-align: center;
    }

    .title {
      padding-top: 2em;
      display: block;
      color: white;
      text-align: center;
      font-weight: 400;
      font-size: x-large;
      -webkit-text-stroke: 1px black;
      text-shadow: 2px 2px black;
    }

    .recent-box {
      position: relative;
      width: 100%;
    }

    .recent-text {
      padding: 0.5em;
      background-color: white;
      position: absolute;
      color: black;
      border: 0.3rem solid black;
      font-size: x-large;
      text-align: center;
      left: 50%;
      transform: translate(-50%, -50%);
    }
  </style>
  <title>Save The Trees!</title>
</head>
<body>